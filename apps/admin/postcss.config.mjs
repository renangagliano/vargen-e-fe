const config = {
  // Admin CSS is intentionally plain CSS. The public site's Tailwind config
  // must not be inherited when Vercel builds with apps/admin as its root.
  plugins: {},
};

export default config;
