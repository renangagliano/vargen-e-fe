import Image from "next/image";
import { notFound } from "next/navigation";
import { Button } from "@/components/button";
import { Badge } from "@/components/badge";
import { Breadcrumbs } from "@/components/breadcrumbs";
import { MusicCard } from "@/components/music-card";
import { JsonLd } from "@/components/json-ld";
import { getSeason } from "@/data/seasons";
import { getSong, songs } from "@/data/songs";
import { absoluteUrl, siteConfig } from "@/config/site";
import { createMetadata } from "@/lib/metadata";

export async function generateStaticParams() { return songs.map((song) => ({ slug: song.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const song = getSong(slug); return song ? createMetadata(`${song.title} — Música Católica`, `${song.description} Referências: ${song.scripture.join(", ")}.`, `/musicas/${song.slug}`) : {}; }

export default async function SongPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const song = getSong(slug); if (!song) notFound(); const season = getSeason(song.liturgicalSeason); const index = songs.findIndex((item) => item.slug === song.slug); const related = songs.filter((item) => item.slug !== song.slug && item.liturgicalSeason === song.liturgicalSeason).slice(0, 2); const jsonLd = { "@context": "https://schema.org", "@type": "MusicRecording", name: song.title, description: song.description, byArtist: { "@type": "MusicGroup", name: siteConfig.shortName }, datePublished: song.releaseDate, url: absoluteUrl(`/musicas/${song.slug}`), genre: "Música católica / Classic Rock", inLanguage: "pt-BR" }; return <><section className="page-content"><div className="container"><Breadcrumbs items={[{ label: "Músicas", href: "/musicas" }, { label: song.title }]} /><div className="song-layout"><div className="song-art"><Image src={song.thumbnail} alt={`Arte da música ${song.title}`} fill priority sizes="(max-width: 920px) 100vw, 42vw" /></div><div className="song-info"><Badge>{season?.name}</Badge><h1>{song.title}</h1><p className="song-info__lead">{song.description}</p><div className="song-meta"><div><span>Celebration</span><strong>{song.celebration}</strong></div><div><span>Ano litúrgico</span><strong>{song.liturgicalYear}</strong></div></div><span className="eyebrow">Referências bíblicas</span><div className="scripture-list">{song.scripture.map((ref) => <span key={ref}>{ref}</span>)}</div><div className="song-actions"><Button href={song.youtubeUrl} external>Ouvir no YouTube</Button>{song.spotifyUrl && <Button href={song.spotifyUrl} variant="outline" external>Spotify</Button>}{song.appleMusicUrl && <Button href={song.appleMusicUrl} variant="outline" external>Apple Music</Button>}</div></div></div><div className="video-embed"><p className="eyebrow">Escute a faixa</p>{song.videoId ? <iframe className="video-embed__iframe" src={`https://www.youtube-nocookie.com/embed/${song.videoId}`} title={`Vídeo de ${song.title}`} loading="lazy" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen /> : <div className="video-embed__fallback"><p className="muted">Vídeo disponível no canal Vargen & Fé</p><Button href={song.youtubeUrl} external>Assistir no YouTube</Button></div>}</div>{related.length > 0 && <div className="related"><h2>Continue a jornada</h2><div className="music-grid">{related.map((item) => <MusicCard key={item.slug} song={item} />)}</div></div>}<div style={{ display: "flex", justifyContent: "space-between", gap: 20, marginTop: 50, paddingTop: 20, borderTop: "1px solid var(--line)" }}>{index > 0 ? <Button href={`/musicas/${songs[index - 1].slug}`} variant="text">← Anterior</Button> : <span />}{index < songs.length - 1 ? <Button href={`/musicas/${songs[index + 1].slug}`} variant="text">Próxima →</Button> : <span />}</div></div></section><JsonLd data={jsonLd} /></>; }
